query GetPeopleQuery {
    allPeople {
        totalCount
        people {
            id
            name
            height
            mass
            homeworld {
                name
                diameter
                gravity
                population
            }
        }
    }
}

query GetPerson($personID: ID) {
    person(personID: $personID) {
        name
    }
}